package com.example.demo.repository;

import com.example.demo.entity.BoardToCard;
import org.springframework.data.jpa.repository.JpaRepository;

public interface BoardToCardRepository extends JpaRepository<BoardToCard, Long> {
}
